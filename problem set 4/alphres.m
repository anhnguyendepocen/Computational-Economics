% -------------------------------------------------------------------------
function res=alphres(alph,gam,prob,r,rf)

res=0;
for i=1:length(prob),
    res=res+prob(i)*((1+rf+alph.*(r(i)-rf)).^(-gam).*(r(i)-rf));
end;
% -------------------------------------------------------------------------

